# 英歌智能体后续优化执行总纲

> 用途：模型切换、长任务续接和防止方向跑偏。后续执行模型必须先完整阅读本文件，再开始修改项目。
>
> 当前日期：2026-08-21
>
> 当前知识版本：`2026.08.16.2`

## 1. 项目最终目标

将项目建设成一个可嵌入不同 H5、回答可追溯、知识可运营、质量可回归、可以正式部署的英歌文化智能体，而不是只有视觉效果的展示网页。

目标优先顺序固定为：

1. 事实准确与证据可追溯；
2. 回答切题、边界清楚、拒绝编造；
3. 后台可以发现、修订和验收问题；
4. 可以安全嵌入其他 H5；
5. 每日知识经过审核后持续增长；
6. 最后进行完整的高级视觉重构。

不得为了视觉效果、完成速度或测试通过率，牺牲前五项。

## 2. 当前已完成基线

执行前不得重复建设以下能力：

- DeepSeek `deepseek-v4-flash` 服务端接入；
- 本地 RAG 检索与真实来源注册；
- 362 个知识切片；
- 150 条黄金回答（WP-07 已完成）；
- 120 条黄金回答评测；
- 91 条可回答检索评测；
- Recall@6 = 1.0；
- 边界路由准确率 = 1.0；
- H5 对话、来源显示和用户反馈；
- JS 悬浮嵌入组件；
- 后台模型、知识、评测、反馈和应用管理页面；
- 高频问题自动聚类与 P0—P3 排序；
- 答案修订工作台；
- 修订稿自动验收门；
- 黄金集写入前的后端强制防绕过；
- 知识补充任务、每日候选和版本回滚骨架。

当前必须保持的质量基线：

| 指标 | 最低要求 |
|---|---:|
| Recall@6 | 0.95 |
| 当前基线 Recall@6 | 1.00 |
| 边界路由准确率 | 0.95 |
| 黄金回答通过率 | 0.80 |
| 错误泛化规避率 | 1.00 |
| 测试结束后的残留反馈记录 | 0 |

## 3. 防跑偏硬规则

以下规则对所有后续工作生效：

1. 不得编造人物、队伍、年代、非遗级别、口令、逐拍动作和阵形细节。
2. 不得把单支队伍个案写成全国英歌统一标准。
3. 不得把内部 Markdown 文件名当作最终用户看到的真实来源。
4. 不得让自动采集内容绕过审核直接进入正式知识库。
5. 不得让模型生成的修订稿自动进入黄金集。
6. 不得通过降低阈值、删除失败用例或缩小测试集让质量门变绿。
7. 不得为测试方便批量删除真实反馈、任务或知识版本。
8. 删除测试数据时只能删除本次明确创建且已确认 ID 的记录。
9. 不得在前端、日志、截图或文档中暴露 API Key。
10. 不得在证据和内容工作未完成时优先进行大规模视觉重构。
11. 不得继续堆叠互不一致的 AI 人物素材。
12. 每个工作包必须完成代码、单测、接口验证、浏览器验证和文档更新后才能标记完成。

## 4. 总体阶段与依赖关系

```text
P0 逐句证据审计
  ↓
P0 高风险知识结构化
  ↓
P0 黄金题与对抗评测扩充
  ↓
P1 正式嵌入 SDK 与生产后端
  ↓
P1 每日知识运营闭环
  ↓
P1 网站高级视觉重构
  ↓
P2 国内部署、灰度与增长分析
```

当前唯一允许立即开始的主任务是 `WP-08 多轮与对抗评测扩充`。WP-01—WP-07 已完成，不得重复建设或回退到已完成工作包。

## 5. 工作包状态表

后续模型每完成一个工作包，必须更新本表和文末变更记录。

| ID | 工作包 | 状态 | 前置依赖 | 优先级 |
|---|---|---|---|---|
| WP-01 | 逐句证据审计数据契约 | COMPLETED | 当前基线 | P0 |
| WP-02 | 逐句证据匹配与风险诊断 | COMPLETED | WP-01 | P0 |
| WP-03 | 证据审计 API | COMPLETED | WP-02 | P0 |
| WP-04 | 证据审计后台页面 | COMPLETED | WP-03 | P0 |
| WP-05 | 证据审计接入黄金验收门 | COMPLETED | WP-04 | P0 |
| WP-06 | 高风险知识结构化 | COMPLETED | WP-05 | P0 |
| WP-07 | 黄金题扩充到 150 条 | COMPLETED | WP-06 | P0 |
| WP-08 | 多轮与对抗评测扩充 | IN_PROGRESS | WP-07 | P0 |
| WP-09 | 正式嵌入 SDK 1.0 | NOT_STARTED | WP-08 | P1 |
| WP-10 | 多应用生产后端 | NOT_STARTED | WP-09 | P1 |
| WP-11 | 每日知识运营闭环 | NOT_STARTED | WP-10 | P1 |
| WP-12 | 网站视觉系统重构 | NOT_STARTED | WP-08 | P1 |
| WP-13 | 国内生产部署 | NOT_STARTED | WP-10、WP-11 | P2 |
| WP-14 | 真实用户灰度与增长分析 | NOT_STARTED | WP-13 | P2 |

## 6. 第一阶段：逐句证据审计

### WP-01：证据审计数据契约

目标：定义“回答事实句—证据片段—真实来源”的稳定结构，不先做页面。

建议新增文件：

- `backend/evidence-audit.mjs`
- `agent/scripts/test_evidence_audit.mjs`
- `agent/generated/evidence_audits/README.md`

每条事实声明至少包含：

```json
{
  "claim_id": "claim-001",
  "text": "槌击可以承担动作与节奏之间的时间标记。",
  "claim_type": "mechanism",
  "risk_level": "medium",
  "evidence_refs": [
    {
      "chunk_id": "chunk-id",
      "source_file": "56_锣鼓动作队形协同机制.md",
      "source_id": "registry-source-id",
      "quote": "与声明直接相关的短证据片段",
      "grade": "A",
      "url": "真实来源链接",
      "support": "direct"
    }
  ],
  "status": "supported"
}
```

固定枚举：

- `claim_type`：`fact`、`mechanism`、`comparison`、`identity`、`date`、`number`、`realtime`、`inference`；
- `risk_level`：`low`、`medium`、`high`、`critical`；
- `support`：`direct`、`partial`、`conflict`、`none`；
- `status`：`supported`、`partial`、`unsupported`、`conflicted`。

风险规则：

- 人物身份、非遗级别、具体日期、数字和实时活动默认为 `high`；
- 无来源的人物与认定信息为 `critical`；
- 机制解释默认为 `medium`；
- 感受性描述和明确标记的推断为 `low`。

验收标准：

- 数据结构可以序列化为 JSON；
- 缺失 `claim_id/text/status` 时测试失败；
- 高风险声明没有直接证据时必须标记 `unsupported`；
- 冲突证据不得自动选择一方；
- 不调用模型也能完成基础校验；
- 不修改现有黄金集和知识版本。

### WP-02：事实句拆分与证据匹配

目标：把答案拆成可审计的事实声明，并与当前 RAG 片段建立映射。

实现要求：

1. 忽略标题、纯过渡句和追问句；
2. 保留包含人物、时间、数字、级别、地区、队伍和机制的句子；
3. 使用已有检索函数找候选片段；
4. 计算文字重合、实体重合和来源等级；
5. 输出直接支持、部分支持、冲突或无支持；
6. 推断必须显式标记为 `inference`；
7. 不得让大模型自行发明证据映射。

验收用例至少覆盖：

- 有明确直接证据；
- 有多个来源共同支持；
- 只有相邻主题但不直接支持；
- 存在来源冲突；
- 含人物身份；
- 含具体数字；
- 否定语境，例如“不能视为全国统一标准”；
- 无事实内容的普通建议句。

量化要求：

- 单元测试不少于 12 个；
- 高风险无证据漏报为 0；
- 否定语境误报为 0；
- 测试不得访问网络。

### WP-03：证据审计 API

建议接口：

- `POST /api/admin/feedback/revisions/audit-evidence`
- `GET /api/admin/feedback/revisions/audits/:message_id`

POST 输入：

- `message_id`
- `answer`
- 可选 `question`

POST 输出：

- 总声明数；
- 支持、部分支持、冲突、无支持数量；
- 逐句声明；
- 证据片段；
- 真实来源信息；
- 是否允许进入自动验收下一步。

安全要求：

- 必须走管理 Token；
- 不接受项目目录之外的来源文件；
- 不返回 API Key；
- 引用片段限制长度；
- message_id 不存在时返回 404；
- 高风险无证据时 `passed=false`。

实现约定：

- 审计服务位于 `backend/evidence-audit-api.mjs`，路由只注入服务端 `retrieve` 与 `sourceRegistry`；客户端传入的 `source_file`、切片或注册表字段全部忽略；
- 审计结果写入 SQLite `evidence_audits` 表（不支持 `node:sqlite` 时使用进程内兼容存储），GET 只按精确 `message_id` 回读；
- `sources` 只返回注册表中存在的 HTTPS 链接，`evidence_chunks.quote` 上限 1000 字符；
- `passed/can_continue` 只有在所有可审计声明均有支持、无高风险缺口、无冲突时才为 `true`；不会自动写入黄金集或知识库；
- 真实 API 验证使用隔离端口和临时互动记录，完成后按明确 ID 清理，生产数据残留为 0。

### WP-04：证据审计后台页面

在 `admin-answer-revisions.html` 中增加独立的“逐句证据”视图，不再创建第三个拥挤页面。

界面结构：

- 左列：修订稿事实句；
- 中列：证据片段与支持程度；
- 右列：来源机构、等级和原始链接；
- 顶部：支持率、高风险缺口、冲突数量；
- 标红：无证据和冲突；
- 标黄：只有部分支持；
- 标绿：直接支持；
- 点击来源打开真实页面；
- 可以一键把无证据声明生成知识任务。

不得：

- 只显示内部 Markdown 文件名；
- 以颜色代替文字状态；
- 自动隐藏冲突证据；
- 在移动端强行保持三列导致横向溢出。

验收标准：

- 桌面、390px 移动端都可用；
- 控制台无未预期错误；
- 无证据声明可生成知识任务；
- 来源链接可点击；
- 测试数据使用精确 message_id 清理。

实现结果：

- 在 `web/admin-answer-revisions.html` 与 `web/admin-answer-revisions.js` 中增加逐句证据卡片，不另建后台页面；
- 支持原回答/修订草稿审计、审计结果回读、支持/部分支持/冲突/无支持状态、可展开短引文和 HTTPS 来源链接；
- 内部 Markdown 文件名不再作为详情页的最终来源展示，统一提示通过逐句审计查看真实来源；
- 新增 `agent/scripts/test_admin_evidence_view.cjs`，桌面端验证证据行与来源链接，390px 验证无横向溢出；临时互动与审计记录已精确清理。

### WP-05：接入黄金验收门

在当前自动验收规则之外增加：

- 所有 `critical` 声明必须 `supported`；
- `high` 声明不得为 `unsupported/conflicted`；
- 总声明支持率不得低于 90%；
- 冲突数量必须为 0，或人工填写冲突处理说明；
- 任何字段修改后重新锁定；
- 绕过前端直接调用黄金集写入接口仍必须被后端拒绝。

不得为了让旧黄金回答通过而降低阈值。旧黄金回答若不符合新门，应进入待补证据队列，不应静默删除。

实现结果：

- 新增 `backend/golden-evidence-gate.mjs`，集中执行 critical/high 风险、90% 支持率、冲突处理和审计答案快照锁定；
- `/api/admin/feedback/revisions/validate` 与 `/api/admin/feedback/promote-golden` 只读取服务器保存的审计，忽略客户端伪造的 `evidence_audit`；
- 黄金审核窗新增“证据冲突处理说明”，任何答案修改后必须重新审计并重新验收；
- 新增 `agent/scripts/test_golden_evidence_gate.mjs` 与 `agent/scripts/test_golden_http_gate.mjs`，覆盖纯逻辑阈值和绕过前端直接写入的拒绝；
- 已完成接口、浏览器、知识库和残留数据回归；黄金集仍为 50 条、评测 20 条、知识版本仍为 `2026.08.16.2`。

## 7. 第二阶段：高风险知识结构化

### WP-06：结构化数据范围

按以下顺序建设：

1. 非遗项目与级别；
2. 代表性传承人；
3. 地区、村落和队伍；
4. 阵形和动作；
5. 锣鼓、吆喝和信号；
6. 实时活动。

建议新增 `agent/structured/`，每类使用 JSON 文件和 Schema 校验。

人物记录必须包含：

- 姓名；
- 项目名称；
- 认定级别；
- 批次或年份；
- 所属地区；
- 官方来源；
- 来源更新时间；
- 是否允许公开展示。

队伍记录必须区分：

- 官方名称；
- 民间俗称；
- 所属村落和地区；
- 已公开特色；
- 不应泛化的队伍个案；
- 来源。

阵形和动作记录不得只有名称，必须包含来源、适用队伍和安全边界。

完成门：

- 所有 JSON 通过 Schema；
- 高风险字段来源覆盖 100%；
- 重名人物和队伍有唯一 ID；
- 旧 Markdown 与结构化数据的冲突有报告；
- RAG 重建后 Recall@6 不低于 0.95。

完成记录（2026-08-21）：

- 新增 `agent/structured/`：项目 3 条、传承人 4 条、队伍 6 条、地方标准阵形 7 条、信号 2 条、历史活动 5 条；
- 新增 `agent/structured/conflicts.json`，记录西门女子队史、甲子保护单位、林忠诚官方序号缺口、同名阵形/槌法四类冲突；
- 新增 `agent/scripts/validate_structured.mjs` 与 `agent/scripts/test_structured.mjs`，校验 JSON、必填字段、唯一 ID、A 级来源覆盖、时间格式、队伍/阵形边界及历史活动安全规则；
- 高风险结构化来源覆盖 100%，所有首批实时类记录均明确为历史记录，未修改黄金集、知识版本或公共网站视觉；
- `python agent/scripts/validate_kb.py` 回归 Recall@6 = 1.0，结构化合同测试与既有证据审计测试全部通过。

## 8. 第三阶段：黄金题和对抗评测

### WP-07：黄金回答扩充

目标：50 条扩充到 150 条，分批进行，每批 20—25 条。

分布建议：

- 基础概念 15；
- 地区与队伍 25；
- 动作与阵形 25；
- 锣鼓与声音 15；
- 脸谱与角色 15；
- 非遗和历史 20；
- 人物身份 10；
- 实时活动 10；
- 儿童学习与安全 10；
- 保护传承 5。

每条必须具备：

- 一个标准问题；
- 至少两个真实问法别名；
- 标准答案；
- 开头切题词；
- 关键事实词；
- 禁止错误表述；
- 来源文件；
- 是否强制适用边界；
- 逐句证据审计结果。

禁止一次性由模型批量生成 100 条后直接写库。必须分批审核和回归。

第一批完成记录（2026-08-21）：

- 新增 `gold-051`—`gold-070` 共 20 条，覆盖项目名称与批次、保护单位边界、代表队伍个案、七种地方标准阵形、锣鼓/吆喝信号边界、人数、传承人和历史活动时效；
- 每条新增记录均包含别名、开头切题词、关键事实词、禁止表述、来源文件和适用边界；
- 黄金评测集同步新增 `GQA-021`—`GQA-040`，总数 40 条；
- 新增 `agent/scripts/test_golden_batch_20260821.mjs`，20 条答案验收全部 100 分；
- `validate_golden_answers.mjs`、`validate_kb.py` 和证据/答案回归全部通过，Recall@6 保持 1.0；
- 为支持“地方标准/历史活动/具体活动”等真实边界表达，验收器边界词规则做了最小增量调整，未降低任何质量门槛。

第二批完成记录（2026-08-21）：

- 新增 `gold-071`—`gold-090` 共 20 条，覆盖角色分工、脸谱辨识与绘制、前棚后棚、舞台/巡游媒介差异、影像证据、锣鼓曲牌、跨文化术语、儿童训练和当代传播；
- 黄金评测集同步新增 `GQA-041`—`GQA-060`，总数 60 条；
- 新增 `agent/scripts/test_golden_batch_20260821_second.mjs`，第二批 20 条答案验收全部 100 分；
- `validate_golden_answers.mjs`、`validate_kb.py`、答案/证据回归全部通过，Recall@6 保持 1.0；
- WP-07 仍未完成，剩余 40 条按相同批次规则继续扩充，不提前进入 SDK、部署或视觉工作。

第三批完成记录（2026-08-21）：

- 新增 `gold-091`—`gold-110` 共 20 条，覆盖板式与人数边界、地域比较、地方标准阵形观察、完整演出生命周期、巡游停演点、观演安全、海外传播、跨文化地域写法、保护与舞台创新、AI素材证据和资料冲突处理；
- 黄金评测集同步新增 `GQA-061`—`GQA-080`，总数 80 条；
- 新增 `agent/scripts/test_golden_batch_20260821_third.mjs`，第三批 20 条答案验收全部 100 分；
- `validate_golden_answers.mjs`、`validate_kb.py`、答案/证据回归全部通过，Recall@6 保持 1.0；
- WP-07 仍未完成，剩余 40 条继续按相同批次规则扩充；完成前不进入 SDK、部署或视觉工作。

第四批完成记录（2026-08-21）：

- 新增 `gold-111`—`gold-130` 共 20 条，覆盖队伍正式名称、成立年份冲突、传承人/保护单位职责、人物隐私、师承谱系、隐性知识、实时活动过期判定、海报与现场证据、时间码、听觉证据、术语差异和视频队伍识别；
- 黄金评测集同步新增 `GQA-081`—`GQA-100`，总数 100 条；
- 新增 `agent/scripts/test_golden_batch_20260821_fourth.mjs`，第四批 20 条答案验收全部 100 分；
- `validate_golden_answers.mjs`、`validate_kb.py`、四批答案/证据回归全部通过，Recall@6 保持 1.0；
- WP-07 仍未完成，剩余 20 条继续按相同批次规则扩充；完成前不进入 SDK、部署或视觉工作。

第五批完成记录（2026-08-21）：

- 新增 `gold-131`—`gold-150` 共 20 条，覆盖传说/历史证据分层、傩文化比较、醒狮辨析、女子英歌、英歌槌制作、视觉协同、锣鼓信号、阵形复现、动作术语、非遗制度、对话路由和长答案引用；
- 黄金评测集同步新增 `GQA-101`—`GQA-120`，总数 120 条；
- 新增 `agent/scripts/test_golden_batch_20260821_fifth.mjs`，第五批 20 条答案验收全部 100 分；
- `validate_golden_answers.mjs`、`validate_kb.py`、五批答案/证据回归全部通过，Recall@6 保持 1.0；
- WP-07 正式完成：黄金集达到 150 条、评测集达到 120 条；下一步切换 WP-08，不提前进入 SDK、部署或视觉工作。

### WP-08：多轮和对抗测试

至少增加：

- 指代追问；
- 地区切换；
- 用户纠错；
- 错误前提；
- 诱导编造人物；
- 诱导提供逐拍统一教程；
- 实时信息过期；
- 联合国非遗误导；
- 相似队伍混淆；
- “是不是所有队都一样”类绝对化问题。

目标：

- 总评测 200—300 条；
- 人物身份误判 0；
- 实时信息冒充 0；
- 错误泛化规避率 1.0；
- 多轮指代正确率不低于 0.90。

首轮完成记录（2026-08-21）：

- 新增 `agent/adversarial_eval.json`，建立 20 条可重复的多轮/对抗回归用例，覆盖地区切换、用户纠错、错误前提、人物身份边界、逐拍教程诱导、安全训练、实时过期、隐私和长答案来源组织；
- 新增 `backend/adversarial-eval.mjs` 纯函数评测器，检查回答深度、必备主题词、路由提示词、禁止表达和内部实现信息泄露；
- 新增 `agent/scripts/test_adversarial_eval.mjs`，20/20 用例通过；该集独立于黄金答案，不降低既有验收门槛；
- 新增 `agent/scripts/run_adversarial_http.mjs`，支持把每条用例逐轮回放到 `/api/admin/test`；默认 dry-run，只有显式 `--live` 才会调用模型，避免回归测试意外消耗额度；
- 第二轮 smoke test 已启动本地服务并确认知识版本、Key 配置和接口契约正常；2 条真实回放均在模型传输层返回 `MODEL_UNAVAILABLE / fetch failed`，未获得模型答案，因此不把它们计入内容质量失败，后续需先处理部署环境到模型端点的连通性。
- WP-08 仍处于进行中，下一轮扩展目标为：把 20 条对抗样例扩展为带真实模型输出的多轮轨迹，并与 150 条黄金集合并评估 200—300 条总集。

## 9. 第四阶段：正式嵌入与生产后端

### WP-09：SDK 1.0

交付一个版本化、可配置、不会污染宿主页面的正式 SDK：

```html
<script
  src="https://static.example.com/yingge-agent/v1/yingge-agent.js"
  data-app-id="museum-h5"
  data-theme="red"
  data-position="right"
></script>
```

必须支持：

- iframe 或 Shadow DOM 隔离；
- app_id；
- 域名白名单；
- 欢迎语和快捷问题；
- 桌面和移动端；
- 来源展开；
- 反馈；
- 版本号；
- 错误回退；
- 无障碍键盘操作；
- 木疙瘩页面接入说明。

### WP-10：生产后端

SQLite 保留本地开发，正式多应用部署迁移 PostgreSQL。

需要：

- 管理员登录；
- 应用密钥；
- 请求签名；
- 限流；
- 会话过期；
- 日志脱敏；
- 请求追踪 ID；
- 模型费用统计；
- 数据库备份；
- 健康检查和告警；
- CORS 白名单。

## 10. 第五阶段：每日知识运营

### WP-11：审核闭环

固定流程：

```text
官方来源发现
→ 去重与来源评级
→ 地区、日期和事件识别
→ 候选池
→ 人工审核
→ 动态知识发布
→ 自动过期
→ 关联问题回归
```

只允许：

- 政府和文旅官方页面；
- 非遗保护机构；
- 权威媒体；
- 学术机构；
- 明确授权的队伍账号；
- 正式活动主办方。

新闻和活动不得直接混入稳定历史知识。

## 11. 第六阶段：网站高级视觉重构

### WP-12：启动条件

只有满足以下条件才启动：

- WP-05 完成；
- WP-06 完成主要高风险数据；
- 黄金回答不少于 100 条；
- 网站核心事实均可追溯；
- 视觉重构不会阻塞智能体上线。

视觉方向固定为：

- 黑漆、朱砂红、银饰、米白纸张；
- 槌轨迹作为统一运动语言；
- IP 只出现在关键节点；
- 真实影像、插画、3D 分层使用；
- 动效服务于动作、阵形、声音和地域叙事。

素材生产固定流程：

```text
角色母版
→ 正侧背标准图
→ 单动作关键帧
→ 统一灯光与材质
→ 视频或骨骼动画
→ 网页合成
```

禁止继续使用：

- 无依据的伪阵形；
- 多张角色不一致的 AI 拼图；
- 简单红圆代替真实内容；
- 大量无功能的发光粒子；
- 不准确的地图标记；
- 把官方标准地图当成可任意倾斜的装饰平面。

## 12. 第七阶段：国内部署与灰度

### WP-13：生产部署

域名不等于大陆可访问。正式上线至少需要：

- 国内可稳定访问的服务器；
- 域名实名认证；
- ICP 备案；
- HTTPS；
- 国内对象存储与 CDN；
- DeepSeek API 连通；
- 数据库备份；
- 隐私说明；
- 管理后台鉴权；
- 故障告警。

### WP-14：真实用户灰度

先选择一个 H5 小流量接入，观察 2—4 周：

- 日活跃用户；
- 问题数量；
- 点踩率；
- 无答案率；
- 低证据率；
- P0/P1 聚类主题；
- 平均响应时间；
- 模型费用；
- 来源点击率；
- 修订后同类问题改善率。

没有真实数据前，不进行没有目标的 A/B 动效实验。

## 13. 每个工作包的固定执行流程

后续模型必须按以下顺序工作：

1. 阅读本文件和相关现有代码；
2. 明确当前只处理哪个工作包；
3. 列出会修改和不会修改的文件；
4. 先写数据契约或测试；
5. 实现后端；
6. 实现后台页面；
7. 运行语法和单元测试；
8. 运行真实 API 验证；
9. 运行桌面和移动端浏览器验证；
10. 精确清理本次测试数据；
11. 运行知识与黄金回答回归；
12. 更新 README、本文件状态和变更记录；
13. 汇报结果及下一个工作包，不跨包顺手大改。

## 14. 固定回归命令

Node 路径按当前工作空间实际配置使用。核心命令：

```powershell
node --check backend/server.mjs
node --check web/admin-answer-revisions.js
node agent/scripts/test_answer_acceptance.mjs
node agent/scripts/test_answer_revisions.mjs
node agent/scripts/test_question_clusters.mjs
node agent/scripts/validate_golden_answers.mjs
python agent/scripts/validate_kb.py
```

完成 WP-01—WP-05 后追加：

```powershell
node agent/scripts/test_evidence_audit.mjs
node agent/scripts/test_evidence_audit_matcher.mjs
node agent/scripts/test_evidence_audit_api.mjs
node agent/scripts/test_golden_evidence_gate.mjs
node agent/scripts/test_admin_evidence_view.cjs
node agent/scripts/test_adversarial_eval.mjs
```

隔离端口启动后再运行 `node agent/scripts/test_golden_http_gate.mjs`，验证直接调用黄金写入接口会被后端拒绝；测试结束后必须确认 `wp05-http-` 记录为 0。

最终还要检查：

- `/api/health`；
- `/api/admin/evaluation/readiness`；
- 测试问答记录为 0；
- 浏览器控制台无未预期错误；
- API Key 没有进入前端文件。

## 15. Luna 切换后的第一条执行指令

切换模型后，不要发送笼统的“继续”。当前应发送：

> 完整阅读 `LUNA_EXECUTION_BRIEF.md`，只执行 WP-06：按既定 Schema 结构化高风险知识（非遗项目、传承人、地区队伍、阵形动作、锣鼓信号、实时活动）。先建立字段契约和来源等级，再做小批量数据与校验；不要扩充黄金集，不要修改公共网站视觉。

历史工作包指令（仅供追溯）：

> 完整阅读 `LUNA_EXECUTION_BRIEF.md`，只执行 WP-01：逐句证据审计数据契约。不要开始 WP-02，不修改公共网站视觉，不修改黄金回答和知识版本。完成代码、单元测试、文档和回归后汇报。

WP-01 完成后再发送：

> 按 `LUNA_EXECUTION_BRIEF.md` 继续执行 WP-02，只做事实句拆分与证据匹配，遵守既定验收标准。

这样能够防止新模型一次跨越多个阶段、重复建设或转向视觉优化。

## 16. 变更记录

| 日期 | 工作包 | 结果 | 质量变化 | 下一项 |
|---|---|---|---|---|
| 2026-08-17 | 执行总纲 | 已建立 | 不修改代码和知识版本 | WP-01 |
| 2026-08-19 | WP-01 | 已完成数据契约、纯校验器、16 条离线契约测试和记录说明；未修改黄金集、知识版本或公共视觉 | 新增 evidence audit 基础校验，支持率/高风险缺口/冲突统计可序列化 | WP-02 |
| 2026-08-19 | WP-02 | 已完成事实句拆分、实体/术语/来源等级匹配、否定与日期批次冲突诊断、14 条离线测试；未接 API、未改黄金集和知识版本 | 高风险无 direct 证据会保持 unsupported，否定边界不会被误报为正面支持 | WP-03 |
| 2026-08-19 | WP-03 | 已完成逐句审计服务、管理 POST/GET API、SQLite/JSON 持久化、来源链接输出、输入与路径注入校验；新增 8 条 API 单测并完成隔离端口 HTTP 回归；未修改黄金集、知识版本和后台页面 | 审计结果可按 message_id 回读，高风险缺证据/冲突不会继续自动验收；长段落证据改为按相关单句比较，降低上下文否定误报 | WP-04 |
| 2026-08-19 | WP-04 | 已在现有答案修订页加入逐句证据视图、可展开引文、真实来源链接、原回答/草稿审计入口与响应式布局；新增桌面/390px 浏览器验收并清理测试记录；未修改黄金集、知识版本和公共网站 | 管理员可以从事实句直接看到证据和来源，内部 Markdown 文件名不再冒充最终来源；移动端无横向溢出 | WP-05 |
| 2026-08-19 | WP-05 | 已将逐句证据审计接入答案验收与黄金写入后端；新增 critical/high/90%/冲突处理/答案快照锁定规则、冲突说明输入和两组对抗测试；回归后黄金集 50 条、评测 20 条、知识版本 `2026.08.16.2` 未变 | 浏览器无法伪造审计绕过黄金集写入；答案字段修改会被后端识别并重新锁定 | WP-06 |
| 2026-08-21 | WP-06 | 完成高风险知识结构化首批数据与 Schema/来源/冲突校验；未扩充黄金集、未改知识版本和公共视觉 | 3 项目、4 传承人、6 队伍、7 阵形、2 信号、5 历史活动；来源覆盖 100%，Recall@6 保持 1.0 | WP-07 |
| 2026-08-21 | WP-07（第一批） | 新增 20 条黄金回答和 20 条评测题，黄金集 50→70、评测 20→40；新增批次验收脚本；未改公共视觉 | 新增条目全部通过答案门禁，Recall@6 保持 1.0；WP-07 仍在进行，后续继续分批扩充到 150 条 | WP-07 第二批 |
| 2026-08-21 | WP-07（第二批） | 新增 20 条黄金回答和 20 条评测题，黄金集 70→90、评测 40→60；补充角色、脸谱、演出结构、声音、跨文化和训练安全 | 新增条目全部通过答案门禁，Recall@6 保持 1.0；WP-07 仍在进行 | WP-07 第三批 |
| 2026-08-21 | WP-07（第三批） | 新增 20 条黄金回答和 20 条评测题，黄金集 90→110、评测 60→80；补充板式/地域比较、地方标准阵形、演出生命周期、观演安全、海外传播与证据冲突处理 | 新增条目全部通过答案门禁，Recall@6 保持 1.0；WP-07 仍在进行 | WP-07 第四批 |
| 2026-08-21 | WP-07（第四批） | 新增 20 条黄金回答和 20 条评测题，黄金集 110→130、评测 80→100；补充人物/队伍身份、成立年份冲突、传承谱系、实时活动时效、隐私授权、多模态证据与术语差异 | 新增条目全部通过答案门禁，Recall@6 保持 1.0；WP-07 仍在进行 | WP-07 第五批 |
| 2026-08-21 | WP-07（第五批） | 新增 20 条黄金回答和 20 条评测题，黄金集 130→150、评测 100→120；补充历史证据分层、表演形态比较、道具制作、信号与阵形复现、非遗制度和对话逻辑 | 五批新增条目全部通过答案门禁，Recall@6 保持 1.0；WP-07 正式完成 | WP-08 |
| 2026-08-21 | WP-08（首轮） | 新增 20 条多轮与对抗回归用例、离线评测器和独立测试脚本；覆盖地区切换、用户纠错、错误前提、人物边界、逐拍教程、安全训练、实时过期、隐私和长答案来源组织 | 对抗集 20/20 通过；不改变黄金集或知识版本；WP-08 继续扩展真实模型多轮轨迹 | WP-08（第二轮） |
| 2026-08-21 | WP-08（第二轮） | 新增真实接口逐轮回放器并完成本地服务 smoke test；不保存模型回答、不输出 Key | 本地服务正常，2 条回放均在上游传输层 `fetch failed`；内容评测暂不计分，先处理模型端点连通性 | WP-08（第二轮重试） |
