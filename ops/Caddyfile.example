# Replace the domain and project path before deploying.
yingge.example.com {
    encode gzip

    handle /api/* {
        reverse_proxy 127.0.0.1:8787
    }

    handle {
        root * D:/vctest/英歌舞/web
        try_files {path} /index.html
        file_server
    }
}
