import * as THREE from './vendor/three.module.js';
const canvas=document.querySelector('#ambientCanvas');if(canvas){const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.1,100);camera.position.set(0,0,12);const group=new THREE.Group();scene.add(group);const pts=[];for(let i=0;i<28;i++){const a=i*.73,r=1.5+(i%7)*.38;pts.push(new THREE.Vector3(Math.cos(a)*r,Math.sin(a)*r*.56,(i%4)*.16))}const geo=new THREE.BufferGeometry().setFromPoints(pts),mat=new THREE.PointsMaterial({color:0xde7622,size:.075,transparent:true,opacity:.85});group.add(new THREE.Points(geo,mat));const lineGeo=new THREE.BufferGeometry().setFromPoints(pts),lineMat=new THREE.LineBasicMaterial({color:0xf04a3a,transparent:true,opacity:.28});group.add(new THREE.Line(lineGeo,lineMat));function resize(){const w=canvas.clientWidth,h=canvas.clientHeight;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix()}let active=true;new IntersectionObserver(([e])=>active=e.isIntersecting,{threshold:.05}).observe(canvas);document.addEventListener('visibilitychange',()=>active=!document.hidden);function render(t){requestAnimationFrame(render);if(!active)return;group.rotation.z=Math.sin(t*.00018)*.08;group.rotation.y=Math.sin(t*.00012)*.12;renderer.render(scene,camera)}resize();addEventListener('resize',resize);requestAnimationFrame(render)}
